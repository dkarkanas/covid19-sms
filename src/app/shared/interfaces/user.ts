export interface User {
    /**
     * The full name of the user to include to the SMS 
     */
    name: string;

    /**
     * The full address of the user to include to the SMS
     */
    address: string;
}